<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wallet.member.mapper.MemberMapper">

    <select id="findByEmail" parameterType="string" resultType="com.wallet.member.domain.Member">
        SELECT
            member_id AS memberId,
            email,
            password_hash AS password,
            simple_password_hash AS simplePasswordHash,
            simple_password_failed_attempt_count AS simplePasswordFailedAttemptCount,
            simple_password_locked_until AS simplePasswordLockedUntil,
            name,
            nickname,
            member_status AS memberStatus,
            created_at AS createdAt,
            updated_at AS updatedAt,
            withdrawn_at AS withdrawnAt
        FROM member
        WHERE email = #{email}
            LIMIT 1
    </select>

    <select id="findById" resultType="com.wallet.member.domain.Member">
        SELECT
            member_id AS memberId,
            email,
            password_hash AS password,
            simple_password_hash AS simplePasswordHash,
            simple_password_failed_attempt_count AS simplePasswordFailedAttemptCount,
            simple_password_locked_until AS simplePasswordLockedUntil,
            name,
            nickname,
            member_status AS memberStatus,
            created_at AS createdAt,
            updated_at AS updatedAt,
            withdrawn_at AS withdrawnAt
        FROM member
        WHERE member_id = #{memberId}
          AND withdrawn_at IS NULL
            LIMIT 1
    </select>

    <!--
        SELECT EXISTS(...)는 조건에 맞는 행이 하나라도 있으면 1,
        없으면 0을 반환한다. MyBatis는 이 값을 boolean으로 변환해준다.
    -->
    <select id="existsByEmail" resultType="boolean">
        SELECT EXISTS (
            SELECT 1
            FROM member
            WHERE email = #{email}
        )
    </select>

    <!--
    JwtAuthenticationFilter 전용 조회. findById와 달리 withdrawn_at 조건을
    걸지 않는다 — 여기서는 탈퇴 여부 자체를 판별해야 하므로, member_status 문자열 하나만 반환한다.
-->
    <select id="findStatusById" resultType="string">
        SELECT member_status
        FROM member
        WHERE member_id = #{memberId}
    </select>

    <update id="anonymizeMember">
        UPDATE member
        SET
            email = #{anonymizedEmail},
            name = #{anonymizedName},
            nickname = #{anonymizedNickname},
            password_hash = #{anonymizedPasswordHash},
            member_status = 'WITHDRAWN',
            withdrawn_at = #{withdrawnAt},
            updated_at = CURRENT_TIMESTAMP
        WHERE member_id = #{memberId}
          AND member_status = 'ACTIVE'
          AND withdrawn_at IS NULL
    </update>

    <!--
        useGeneratedKeys="true":
        - MySQL의 AUTO_INCREMENT로 생성된 member_id를 자바 객체에 다시 채워준다.
    -->
    <insert id="insertMember"
            parameterType="com.wallet.member.domain.Member"
            useGeneratedKeys="true"
            keyProperty="memberId">
        INSERT INTO member (
            email,
            password_hash,
            name,
            nickname,
            member_status
        )
        VALUES (
                   #{email},
                   #{password},
                   #{name},
                   #{nickname},
                   #{memberStatus}
               )
    </insert>

    <update id="updateMemberInfo">
        UPDATE member
        SET
            nickname = #{nickname},
            updated_at = CURRENT_TIMESTAMP
        WHERE member_id = #{memberId}
          AND member_status = 'ACTIVE'
          AND withdrawn_at IS NULL
    </update>

    <update id="updatePassword">
        UPDATE member
        SET
            password_hash = #{encodedPassword},
            updated_at = CURRENT_TIMESTAMP
        WHERE member_id = #{memberId}
          AND member_status = 'ACTIVE'
          AND withdrawn_at IS NULL
    </update>

    <!--
        활성 상태이고 탈퇴하지 않은 회원만 수정한다. 조건에 맞지 않으면 수정 건수가 0이 되므로
        Service에서 저장 실패를 정상 성공으로 잘못 판단하지 않게 할 수 있다.
    -->
    <update id="updateSimplePassword">
        UPDATE member
        SET
            simple_password_hash = #{simplePasswordHash},
            simple_password_failed_attempt_count = 0,
            simple_password_locked_until = NULL,
            updated_at = CURRENT_TIMESTAMP
        WHERE member_id = #{memberId}
          AND member_status = 'ACTIVE'
          AND withdrawn_at IS NULL
    </update>

    <update id="increaseSimplePasswordFailedAttemptCount">
        UPDATE member
        SET
            simple_password_failed_attempt_count = simple_password_failed_attempt_count + 1,
            updated_at = CURRENT_TIMESTAMP
        WHERE member_id = #{memberId}
          AND member_status = 'ACTIVE'
          AND withdrawn_at IS NULL
    </update>

    <!-- 다섯 번째 실패를 기록하는 동시에 잠금 만료시각을 저장한다. -->
    <update id="lockSimplePasswordVerification">
        UPDATE member
        SET
            simple_password_failed_attempt_count = 5,
            simple_password_locked_until = #{lockedUntil},
            updated_at = CURRENT_TIMESTAMP
        WHERE member_id = #{memberId}
          AND member_status = 'ACTIVE'
          AND withdrawn_at IS NULL
    </update>

    <update id="resetSimplePasswordVerificationFailure">
        UPDATE member
        SET
            simple_password_failed_attempt_count = 0,
            simple_password_locked_until = NULL,
            updated_at = CURRENT_TIMESTAMP
        WHERE member_id = #{memberId}
          AND member_status = 'ACTIVE'
          AND withdrawn_at IS NULL
    </update>

    <!--
    SELECT ... FOR UPDATE는 조회한 행에 쓰기 잠금을 건다.

    이 쿼리는 반드시 @Transactional이 적용된 Service 메서드 안에서 호출해야 한다.
    트랜잭션이 끝날 때까지 같은 member_id에 대한 다른 FOR UPDATE 요청은 대기하게 된다.
    -->
    <select id="lockActiveMemberById"
            resultType="long">
        SELECT member_id
        FROM member
        WHERE member_id = #{memberId}
          AND member_status = 'ACTIVE'
          AND withdrawn_at IS NULL
            FOR UPDATE
    </select>
</mapper>
