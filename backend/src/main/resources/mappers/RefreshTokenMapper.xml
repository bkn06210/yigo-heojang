<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wallet.auth.mapper.RefreshTokenMapper">

    <insert id="insert">
        INSERT INTO refresh_token (
            member_id,
            token_hash,
            expires_at
        )
        VALUES (
                   #{memberId},
                   #{tokenHash},
                   #{expiresAt}
               )
    </insert>

    <select id="findValidTokenByHash" resultType="com.wallet.auth.domain.RefreshToken">
        SELECT
            refresh_token_id AS refreshTokenId,
            member_id AS memberId,
            token_hash AS tokenHash,
            expires_at AS expiresAt,
            revoked_at AS revokedAt,
            revoke_reason AS revokeReason,
            created_at AS createdAt
        FROM refresh_token
        WHERE token_hash = #{tokenHash}
          AND revoked_at IS NULL
          AND expires_at > NOW()
            LIMIT 1
    </select>

    <update id="revokeByHash">
        UPDATE refresh_token
        SET
            revoked_at = NOW(),
            revoke_reason = #{revokeReason}
        WHERE token_hash = #{tokenHash}
          AND revoked_at IS NULL
    </update>

    <update id="revokeAllByMemberId">
        UPDATE refresh_token
        SET
            revoked_at = NOW(),
            revoke_reason = #{revokeReason}
        WHERE member_id = #{memberId}
          AND revoked_at IS NULL
    </update>

</mapper>