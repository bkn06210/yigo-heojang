<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/mvc
           http://www.springframework.org/schema/mvc/spring-mvc.xsd
           http://www.springframework.org/schema/context
           http://www.springframework.org/schema/context/spring-context.xsd">

    <!-- 어노테이션 기반 MVC 활성화.
         jackson-databind가 클래스패스에 있으면 JSON 메시지 컨버터를 자동 등록한다.
         → 별도 뷰리졸버(JSP 등) 없이 @RestController 반환값이 JSON으로 나간다. -->
    <mvc:annotation-driven/>

    <context:property-placeholder location="classpath:db.properties"/>

    <!-- CORS 전역 설정. 개발 환경에서는 localhost의 모든 포트를 허용한다.
         운영 배포 시에는 실제 프론트 도메인으로 변경해야 한다. -->
    <mvc:cors>
        <mvc:mapping path="/api/**"
                     allowed-origins="http://localhost:5173,http://localhost:5174,http://localhost:5175,http://localhost:5176,http://localhost:5177,http://localhost:5178,http://localhost:5179"
                     allowed-methods="GET,POST,PUT,PATCH,DELETE,OPTIONS"
                     allowed-headers="*"
                     allow-credentials="true"
                     max-age="3600"/>
    </mvc:cors>

    <!-- 컨트롤러만 스캔 (서비스 계층은 root-context가 담당).
         use-default-filters=false + @Controller 포함 → @RestController도 함께 등록됨. -->
    <context:component-scan base-package="com.wallet" use-default-filters="false">
        <context:include-filter type="annotation"
            expression="org.springframework.stereotype.Controller"/>
    </context:component-scan>

</beans>
