# 챗봇 서버 의존성. 수집 파이프라인(ingest)과는 따로 관리한다.
#   pip install -r chatbot/requirements.txt
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
python-dotenv>=1.0
httpx>=0.27
# 별칭·가맹점 조회와 약관 원문용. 계산값은 DB 가 아니라 엔진 API 에서 받는다.
pymysql>=1.1
# 약관 검색의 유사도 계산. 조각 전체를 한 번에 곱해 순수 파이썬보다 500배 빠르다
# (실측 1,550개 기준 208ms → 0.4ms). 조각 수에 정비례하는 값이라 카드가 늘수록 벌어진다.
numpy>=2.0
# 약관 검색을 별도 벡터 서버로 돌릴 때(TERM_INDEX=qdrant)만 쓴다. 기본(numpy)에서는 부르지 않는다.
qdrant-client>=1.19
# 실제 LLM 을 켤 때만 필요하다. stub 으로 개발하는 동안에는 없어도 서버가 뜬다.
openai>=1.59

# 테스트
pytest>=8.3
