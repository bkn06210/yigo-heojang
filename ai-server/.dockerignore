# 이미지에 담지 않을 것.
# 가상환경과 수집 산출물은 수백 MB라, 안 빼면 빌드가 느려지고 이미지가 불필요하게 커진다.
.venv/
__pycache__/
*.pyc

# 수집 파이프라인은 컨테이너에서 돌지 않는다.
ingest/

# 원본 약관과 중간 산출물
**/raw/
**/out/

# 설정은 파일로 넣지 않고 compose 가 환경변수로 준다 — 이미지에 키가 박히지 않게.
.env
